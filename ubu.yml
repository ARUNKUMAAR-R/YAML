apiVersion: v1
kind: Pod
metadata:
  name: ubuntu
  namespace: nm
spec:
  containers:
    - name: ubuntu-container
      image: ubuntu:latest
      command: ["sleep", "3600"]
      # You can add additional commands or arguments if needed

